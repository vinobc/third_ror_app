<center><h1>Sign Up</h1></center>

<%= form_for(@user) do |f| %>

      <%= render 'shared/error_messages', :object=>f.object %>
      <%= render 'fields', :f=>f %>

	  <div class="actions">
		<%= f.submit "Sign up" %>
	  </div>
	
<% end %>

